From 44ca6fe699d203bb00f2c4ce435f524defdcbc6f Mon Sep 17 00:00:00 2001
From: Wout Stiens <71498452+StiensWout@users.noreply.github.com>
Date: Tue, 25 Aug 2026 11:11:23 +0200
Subject: [PATCH] fix(pi): isolate session MCP credentials

---
 .../src/orchestration-v2/Adapters/piT3McpInjection.test.ts | 6 +++++-
 .../src/orchestration-v2/Adapters/piT3McpInjection.ts      | 7 ++++++-
 2 files changed, 11 insertions(+), 2 deletions(-)

diff --git a/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.test.ts b/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.test.ts
index c29637055..7331cc2ff 100644
--- a/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.test.ts
+++ b/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.test.ts
@@ -59,7 +59,10 @@ describe("pi T3 MCP injection", () => {
 
     const permissionOnly = buildPiRpcLaunch({
       launchArgs: [],
-      environment: {},
+      environment: {
+        [T3_MCP_URL_ENV]: "http://127.0.0.1:9999/stale",
+        [T3_MCP_BEARER_ENV]: "stale-token",
+      },
       mcpSession: undefined,
       extensionPath: "/tmp/cache/pi-t3-mcp-extension.ts",
       runtimeMode: "auto-accept-edits",
@@ -72,6 +75,7 @@ describe("pi T3 MCP injection", () => {
     ]);
     assert.isFalse(permissionOnly.hasT3Mcp);
     assert.isUndefined(permissionOnly.env[T3_MCP_URL_ENV]);
+    assert.isUndefined(permissionOnly.env[T3_MCP_BEARER_ENV]);
     assert.equal(permissionOnly.env[T3_PI_RUNTIME_MODE_ENV], "auto-accept-edits");
   });
 
diff --git a/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.ts b/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.ts
index 0f382ba3f..06edebfde 100644
--- a/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.ts
+++ b/apps/server/src/orchestration-v2/Adapters/piT3McpInjection.ts
@@ -260,11 +260,16 @@ export function buildPiRpcLaunch(input: {
   ) {
     args.push("--extension", input.extensionPath);
   }
+  const environment = { ...input.environment };
+  // These values belong to the current T3 session. Never let a Pi child reuse
+  // credentials inherited from the server or a parent provider process.
+  delete environment[T3_MCP_URL_ENV];
+  delete environment[T3_MCP_BEARER_ENV];
 
   return {
     args,
     env: {
-      ...input.environment,
+      ...environment,
       ...(hasT3Extension && input.runtimeMode !== undefined
         ? { [T3_PI_RUNTIME_MODE_ENV]: input.runtimeMode }
         : {}),
-- 
2.50.1 (Apple Git-155)

